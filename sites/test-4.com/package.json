{
  "name": "test-4.com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//deploy": "Opt this site into CI deploys. The build carries the Cloudflare adapter, so it emits dist/server/wrangler.json and ships as a Worker (static assets + the /api/quote route). CloudCannon only edits the content — it never builds or hosts this site.",
  "deploy": {
    "enabled": true
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "astro check"
  },
  "dependencies": {
    "@astro-fleet/config": "workspace:*",
    "@astro-fleet/shared-ui": "workspace:*",
    "@astrojs/cloudflare": "13.2.0",
    "astro": "^6.1.7",
    "embla-carousel": "^8.5.2",
    "intl-tel-input": "^25.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9",
    "@astrojs/sitemap": "^3",
    "@tailwindcss/vite": "^4",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
